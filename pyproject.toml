[build-system]
requires = []
build-backend = "setuptools.build_meta"

[project]
name = "reusable-core-verification-v1"
version = "0.1.0"
description = "Experimental shadow verification slice with mandatory hard verification"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
verification-v1 = "verification_v1.cli:main"
